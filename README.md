# OneTRIP prototype

Interactive prototype of OneTRIP, the shared operating system for group trips.

Static site with no build step: open `index.html` or visit the GitHub Pages URL.
Demo data only. Nothing is saved between page loads.
